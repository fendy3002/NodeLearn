let initialData = {
    "one": "1",
    "two": "2",
    "three": "3"
};

// the syntax below
let {one, two} = initialData;

// is the same as below
let one = initialData.one;
let two = initialData.two;

// that's it